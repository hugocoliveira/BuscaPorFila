package br.com.lit.busca.fila.ui.theme

import androidx.compose.ui.graphics.Color

// ---------------------------------------------------------------------------
// Paleta de cores do projeto — idêntica ao MenuAutomatico para consistência
// visual entre os aplicativos da suite LIT.
// ---------------------------------------------------------------------------

/** Cor primária — usada em TopAppBar, botões principais e itens ativos. */
val Primary = Color(0xFF051FC7)

/** Container da primária — fundo de cards selecionados e chips. */
val PrimaryContainer = Color(0xFFD6DCFF)

/** Texto/ícone sobre fundo Primary. */
val OnPrimary = Color(0xFFFFFFFF)

/** Fundo geral das telas — branco levemente azulado. */
val Surface = Color(0xFFF8F9FF)

/** Fundo de cards e itens de lista. */
val SurfaceVariant = Color(0xFFE8EAFF)

/** Texto principal sobre Surface. */
val OnSurface = Color(0xFF0D0F1C)

/** Texto secundário e subtítulos. */
val OnSurfaceVariant = Color(0xFF44475A)

/** Cor de erro — mensagens e estados de falha. */
val ErrorColor = Color(0xFFBA1A1A)

/** Bordas e divisores sutis. */
val Outline = Color(0xFFC4C6D0)
